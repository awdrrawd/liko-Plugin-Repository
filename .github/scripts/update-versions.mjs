// Compatibility entry for existing local commands. Version resolution now belongs
// to the catalog build; do not post-process the generated catalog separately.
await import('../../scripts/build.mjs');
