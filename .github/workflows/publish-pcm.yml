name: Build PCM bundle

on:
  push:
    branches: [ main ]
    paths:
      - 'src/pcm/**'
      - 'scripts/build-pcm.mjs'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-pcm-bundle
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci
      - run: npm run build:pcm

      - name: Commit bundle
        run: |
          if [[ -n "$(git status --porcelain dist/pcm/PCM.js)" ]]; then
            git config user.name  "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add dist/pcm/PCM.js
            git commit -m "chore: build PCM bundle"
            git push
          else
            echo "PCM bundle already up to date"
          fi
